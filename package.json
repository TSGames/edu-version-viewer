{
  "name": "edu-version-viewer",
  "version": "1.0.0",
  "description": "Viewer that polls edu-sharing _about endpoints and shows version, last sync, services/modules and features. Vanilla frontend; Fastify backend.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/session": "^11.1.1",
    "@fastify/static": "^9.1.3",
    "fastify": "^5.9.0"
  }
}
