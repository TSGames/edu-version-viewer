apiVersion: v2
name: edu-version-viewer
description: >-
  Dependency-free viewer that polls edu-sharing _about endpoints and shows
  version, last sync, services/modules, features and plugins.
type: application
# version (chart) and appVersion are overridden by CI at package time
# (helm package --version / --app-version) from the git tag. The values here
# are the local/dev defaults and are kept in lockstep.
version: 1.0.0
appVersion: "1.0.0"
home: https://github.com/tsgames/edu-version-viewer
sources:
  - https://github.com/tsgames/edu-version-viewer
keywords:
  - edu-sharing
  - monitoring
  - version
maintainers:
  - name: tsgames
