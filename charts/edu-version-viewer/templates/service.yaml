apiVersion: v1
kind: Service
metadata:
  name: {{ include "edu-version-viewer.fullname" . }}
  labels:
    {{- include "edu-version-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edu-version-viewer.selectorLabels" . | nindent 4 }}
