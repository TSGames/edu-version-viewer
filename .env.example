# Copy to .env and adjust. Used by docker-compose.
ADMIN_USER=admin
ADMIN_PASSWORD=change-me
# Optional read-only account (leave password empty to disable it).
VIEWER_USER=viewer
VIEWER_PASSWORD=
CRON_SCHEDULE=*/15 * * * *
REQUEST_TIMEOUT_MS=10000
PORT=3000
DATA_DIR=/data
